<?xml version="1.0" encoding="utf-8"?>
<resources>
    <!-- 主题样式 -->
    <style name="AppTheme" parent="@style/Theme.AppCompat.Light.DarkActionBar">
        <item name="android:windowContentOverlay">@null</item>
        <item name="android:textSize">@dimen/text_size_m</item>
        <item name="windowActionBar">false</item>
        <item name="windowNoTitle">true</item>
        <item name="colorPrimary">@color/common_black</item>
        <item name="colorPrimaryDark">@color/common_black</item>
        <item name="colorAccent">@color/color_accent</item>
    </style>

    <!-- 通用列表样式-->
    <style name="commonRecyclerView">
        <item name="android:scrollbars">none</item>
        <item name="android:layout_width">match_parent</item>
        <item name="android:layout_height">match_parent</item>
        <item name="android:overScrollMode">never</item>
    </style>

    <!-- 横向分隔线 -->
    <style name="DividerFixedHeight">
        <item name="android:background">@color/common_divider</item>
        <item name="android:layout_width">match_parent</item>
        <item name="android:layout_height">@dimen/common_divider</item>
    </style>

    <!-- 竖向分隔线 -->
    <style name="DividerFixedWidth">
        <item name="android:background">@color/common_divider</item>
        <item name="android:layout_width">@dimen/common_divider</item>
        <item name="android:layout_height">match_parent</item>
    </style>

    <!-- 竖向分隔线 带上下间距 -->
    <style name="DividerFixedWidth.MarginTopBottom" parent="@style/DividerFixedWidth">
        <item name="android:layout_marginTop">@dimen/common_margin_m</item>
        <item name="android:layout_marginBottom">@dimen/common_margin_m</item>
    </style>
</resources>
